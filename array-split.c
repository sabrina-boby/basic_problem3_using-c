#include<stdio.h>
int main()
{
    char a[20];

    for(i=0; i<k; i++)
    {
        scanf("%c ", &a[i]);
    }

    scanf("%d ", &p);

    for(i=0; i<=k; i++)
    {
        printf(" i =%d\n",i);
        printf("%c",a[i]);
    }

      //for(i=0; i<=p; i++)
    //{

    //}
}
